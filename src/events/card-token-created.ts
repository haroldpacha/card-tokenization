import { schema } from '@schemas/card-token.schema';

export const eventName = 'CardTokenCreated';
export const eventSource = 'com.card-token-card-tokenization';
export const eventSchema = schema;
export const eventVersion = '1';
